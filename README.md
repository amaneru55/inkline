# InkNest

InkNest 是一个本地优先的跨平台漫画阅读器项目，产品名为 Inkline，中文名为“墨线”。它的目标是在 Windows、macOS、Linux、Android 和 iOS 上提供一致、顺滑、可扩展的漫画阅读体验。

项目同时面向本地漫画收藏、在线漫画服务、网盘资源和 AI 辅助阅读场景：既能管理本地压缩包和图片文件夹，也能接入常见在线漫画源与个人云盘；既能作为轻量阅读器，也能通过本地模型完成 OCR、翻译和嵌字，帮助用户阅读不同语言的漫画。

## 命名

- 项目名：InkNest
- 产品名：Inkline
- 中文名：墨线
- 副标题：A private comic reader by InkNest
- 定位：Local-first comic reader with OCR translation

## 核心目标

- 跨平台：桌面端优先支持 Windows、macOS、Linux，移动端逐步覆盖 Android 和 iOS。
- 本地优先：支持离线阅读、隐私保护和本地资料库管理，不强依赖云端服务。
- 多来源统一：将本地文件、在线漫画服务和网盘内容整理为一致的书架、阅读记录和搜索体验。
- AI 辅助阅读：使用本地模型完成文字识别、翻译、排版分析和图像嵌字，尽量减少隐私暴露。
- 可扩展：在线源、网盘、翻译器、OCR 引擎和模型后端都应采用插件化或适配器设计。

## Roadmap

架构设计见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。

工程约定见：

- [docs/FRONTEND_ARCHITECTURE.md](docs/FRONTEND_ARCHITECTURE.md)：前端分层、技术栈和组件模式。
- [docs/BOUNDARIES.md](docs/BOUNDARIES.md)：前端、Rust 后端和运行时校验边界。
- [docs/CODE_STYLE.md](docs/CODE_STYLE.md)：TypeScript、Rust、文件组织和不确定性处理规则。
- [docs/UI_SYSTEM.md](docs/UI_SYSTEM.md)：HeroUI token、主题、标题栏和 Storybook 要求。
- [docs/I18N.md](docs/I18N.md)：国际化结构和文案规则。
- [docs/TESTING.md](docs/TESTING.md)：测试策略、覆盖率目标和性能回归检查。

## 当前状态

- 已完成 Tauri + React + TypeScript 基础应用骨架。
- 已接入 Vite、Tailwind CSS、HeroUI、Motion、TanStack Query/Router/Virtual、i18next、Biome、Vitest 和 Storybook。
- 已固定 Volta Node 版本与 pnpm package manager 版本。
- 已建立主题系统：`themeName` 与 `themeMode` 分离，默认跟随系统明暗模式。
- 已建立基础 i18n、Storybook story 和 Vitest 测试。
- 已生成并替换 Windows、macOS、Linux、Android、iOS 所需的应用图标资源。
- 已接入桌面端基础壳能力：原生菜单、system tray、关闭到托盘、窗口状态保存与恢复。
- 已提供跨平台 `Makefile` 常用命令。

### 0. 项目基础

- [x] 搭建基础应用框架：Tauri + React + TypeScript，明确桌面端和未来移动端的共享边界。
- [x] 建立前端工程规范：Biome、Vitest、Storybook、Tailwind、HeroUI、TanStack、Motion 和 i18n 的项目级约束。
- [ ] 建立统一的数据模型：漫画、章节、页、来源、阅读进度、标签、收藏、历史记录。
- [ ] 设计本地数据库结构，用于保存书架、元数据、索引、阅读记录和任务状态。
- [ ] 建立基础设置页：语言、主题、缓存路径、阅读偏好、代理、快捷键和隐私选项。
- [x] 建立桌面基础壳能力：原生菜单、托盘图标、关闭到托盘和窗口状态保存/恢复。
- [ ] 完成打包与发布流程：Windows、macOS、Linux 的安装包、便携包和自动更新策略。

### 1. 本地漫画阅读

- [ ] 支持常见压缩包格式：zip、cbz、rar、cbr、7z、tar。
- [ ] 支持图片文件夹导入，并正确处理自然排序、嵌套目录、封面识别和章节拆分。
- [ ] 支持常见图片格式：jpg、jpeg、png、webp、avif、gif。
- [ ] 提供本地书架：扫描目录、手动导入、移除记录、重新索引、封面刷新。
- [ ] 提供元数据管理：标题、作者、状态、标签、分组、评分、阅读状态。
- [ ] 支持大文件和长篇漫画的按需解压、分页缓存与预加载，避免一次性占用过多内存。
- [ ] 支持损坏文件、缺页、重复页和异常排序的提示与修复工具。

### 2. 阅读器体验

- [ ] 支持单页、双页、长条、右到左、左到右、竖向滚动等阅读模式。
- [ ] 支持缩放、适宽、适高、原始尺寸、旋转、裁边和背景色设置。
- [ ] 支持触控、鼠标、键盘、手柄和快捷键操作。
- [ ] 支持进度记忆、章节跳转、页码定位、继续阅读和最近阅读。
- [ ] 支持书签、页内标注、截图、复制页面、导出选中页。
- [ ] 支持图像增强：亮度、对比度、锐化、去噪、灰度、反色、自动裁白边。
- [ ] 支持低配置设备的性能模式与高分屏设备的清晰渲染模式。

### 3. 在线漫画服务

- [ ] 设计在线源适配器接口，统一搜索、详情、章节、分页、下载和登录能力。
- [ ] 支持常见在线漫画服务，并允许按地区、语言和内容类型扩展。
- [ ] 支持账号登录、Cookie、Token、代理和请求限速配置。
- [ ] 支持在线搜索、订阅更新、收藏同步、章节更新提醒。
- [ ] 支持在线章节缓存、离线下载、断点续传和下载队列管理。
- [ ] 支持来源失效检测、反爬限制提示和适配器更新机制。
- [ ] 提供源管理界面：启用、禁用、排序、测试连接、查看权限与缓存占用。

### 4. 网盘与远程文件

- [ ] 抽象网盘适配器接口，统一浏览、搜索、打开、下载、缓存和权限管理。
- [ ] 支持常见网盘和远程文件协议，例如 WebDAV、S3 兼容存储、SMB、FTP/SFTP。
- [ ] 支持用户常见个人网盘服务的接入，优先实现开放 API 或 WebDAV 兼容方案。
- [ ] 支持远程压缩包按需读取，尽量避免完整下载后才能开始阅读。
- [ ] 支持网盘目录映射为书架，并可选择离线缓存策略。
- [ ] 支持云端文件变更检测、缓存失效、冲突提示和本地索引重建。
- [ ] 支持凭据加密保存，并提供一键登出和清理缓存。

### 5. AI OCR、翻译与嵌字

- [ ] 建立 AI 任务流水线：页面预处理、气泡检测、文字区域检测、OCR、翻译、排版、嵌字、结果审阅。
- [ ] 支持本地 OCR 模型，优先覆盖日文、中文、英文和韩文漫画文本。
- [ ] 支持本地翻译模型，同时保留接入外部翻译服务的可选适配器。
- [ ] 支持术语表、人名表、固定译名、禁翻词和风格提示词。
- [ ] 支持对话气泡、旁白、拟声词、竖排文字、弯曲文字和多列文本的识别策略。
- [ ] 支持嵌字输出：自动清除原文、匹配字体风格、自动换行、描边、阴影和气泡内对齐。
- [ ] 支持人工校对界面：查看原文、译文、置信度、区域框，手动改译和调整排版。
- [ ] 支持结果缓存与版本管理，避免重复识别和翻译同一页面。
- [ ] 支持批量任务：整本翻译、指定章节翻译、后台队列、暂停恢复、失败重试。
- [ ] 支持隐私模式：所有 OCR、翻译、缓存和嵌字都在本地完成。

### 6. 资料库、同步与多端

- [ ] 支持本地资料库索引、全文搜索、标签筛选、作者筛选和高级排序。
- [ ] 支持跨设备阅读进度同步，可选择本地网络同步、WebDAV 同步或自建服务。
- [ ] 支持设置、书架、阅读记录、书签、标注和 AI 结果的可选同步。
- [ ] 支持备份与恢复：导出资料库索引、阅读记录、配置和适配器设置。
- [ ] 支持多用户或多配置文件，区分个人书架、儿童模式和工作目录。
- [ ] 支持移动端离线包管理，控制缓存大小、自动清理和仅 Wi-Fi 下载。

### 7. 插件与开发者能力

- [ ] 设计插件系统，覆盖在线源、网盘、元数据抓取、翻译器、OCR 引擎和图像处理模块。
- [ ] 提供插件权限模型，限制网络、文件、凭据和本地模型访问。
- [ ] 提供插件开发文档、示例插件和调试工具。
- [ ] 支持插件市场或插件仓库索引，允许用户安装、更新和禁用插件。
- [ ] 支持适配器测试套件，降低在线源变更后的维护成本。
- [ ] 提供日志、诊断报告和可脱敏的问题反馈导出。

### 8. 安全、隐私与可维护性

- [ ] 本地凭据加密存储，避免明文保存 Cookie、Token 和网盘密码。
- [ ] 明确网络请求边界，所有在线源、同步和外部翻译服务都应可关闭。
- [ ] 对本地模型、插件和外部服务显示清晰的权限说明。
- [ ] 建立崩溃恢复、任务恢复和数据库迁移机制。
- [ ] 所有新增依赖在引入前确认最新稳定版本、官方文档和当前项目版本兼容性。
- [ ] 建立自动化测试：核心数据模型、文件解析、阅读器状态、适配器协议和 AI 流水线。
- [ ] 前端核心逻辑、hooks、组件交互和 UI story 以接近 100% 覆盖率为目标。
- [ ] 建立性能基准：启动速度、翻页延迟、内存占用、缓存命中率和批量任务吞吐。
- [ ] 建立多平台发布检查清单，覆盖安装、升级、卸载、权限和系统集成。

## 近期优先级

1. 完成本地漫画导入与基础阅读器。
2. 建立书架、阅读记录和本地数据库。
3. 实现压缩包读取、图片文件夹读取和分页缓存。
4. 打磨桌面端阅读体验与基础设置。
5. 设计在线源、网盘和 AI 流水线的适配器接口。
6. 原型验证本地 OCR、翻译和嵌字闭环。

## 开发

```bash
make install
make tauri-dev
```

常用命令：

```bash
make dev              # 启动 Vite dev server
make tauri-dev        # 启动 Tauri 桌面 dev app
make sb               # 启动 Storybook
make check            # 运行 Biome 检查
make test             # 运行 Vitest
make build            # 构建前端
make rust-check       # 运行 cargo check
make verify           # 运行主要验证任务
```

直接使用 pnpm 也可以：

```bash
pnpm sb
pnpm storybook
pnpm tauri dev
```

构建桌面应用：

```bash
make tauri-build
```

## 当前基础设施决策

- 当前不急于展开完整设置页 UI，优先完成可靠的应用设置、资料库、导入和阅读器基础能力。
- 应用级偏好设置通过 Tauri command 持久化到系统 app config 目录下的 `settings.json`，不使用 `localStorage` 作为真实应用存储。
- `themeName` 和 `themeMode` 分离：主题表示 token 套装，外观模式表示 `system` / `light` / `dark`。
- 语言、主题、关闭行为等轻量偏好先进入 app settings；书架、阅读进度、任务、缓存索引、AI 结果等业务数据后续进入 SQLite。
- Storybook 和测试使用内存 settings store，避免污染真实用户配置。
- 已建立 `core/library` 和 `core/reader` 的第一版纯 TypeScript 模型与测试，用于承接后续本地导入、资料库和阅读器实现。
- Zod 作为前端不可信边界的运行时校验库；开发期 pnpm `minimumReleaseAge` 设为 60 分钟，发布前再提高安全窗口。
